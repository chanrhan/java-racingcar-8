### docs: 구현해야할 기능을 명세한 README 파일 추가

### feat: 기본 구조 설계 

### feat: 기본 입출력 구조 설계

- 자동차 이름을 포함한 문자열과 주어진 횟수를 입력받아 `RaceManager` 객체 생성
  - 자동차 이름을 포함한 문자열은 파싱되어 멤버 변수인 `cars`에 저장 
- 매 차수마다 모든 `Car`의 이동 현황을 터미널에 출력
- 모든 차수가 끝나면, 가장 많이 이동한 자동차(들)를 최종 우승자로 출력  

### feat: 자동차 움직임 구현

- 매 차수마다 모든 `Car`의 `tryMove()` 함수를 호출
  - `tryMove()` 함수에서는 무작위 0~9사이의 값 중 4이상이 값이 나올 때만 true 를 반환하는 `canMove()`로 움직일지 여부를 판별
- 각 차수마다 모든 `Car`의 움직임 시도가 끝났다면, 모든 자동차들의 위치 현황을 터미널에 출력

### feat: 우승자 결정 구현 

- 모든 차수가 완료되었다면, 자동차들을 멀리 간 순서대로 내침차순 정렬한 뒤, 
  가장 멀리 간 자동차(들)를 `,`를 구분자로 `join()`하여 터미널에 출력

### feat: 예외 처리 

- 우승자 결정 과정에서의 예외 처리
- 자동차 이름을 포함한 문자열을 입력받을 때, 자동차 이름이 5자를 초과했다면 예외 처리 

### fix: 랜덤 정수를 생성하는 라이브러리 교체

- 기본 `java.util.Random` 라이브러리 대신, 제공된 라이브러리인 `camp.nextstep.edu.missionutils.Randoms` 사용 

### refactor: 패키지 재설계 및 단일 책임 분리 적용

- `RaceService` 
  - 클래스명 변경 
    - 기존 : `RaceManager`
    - 변경 : `RaceService`
  - Race 를 실행하는 역할만 담당하도록 코드 분리
    - 문자열을 파싱하거나, 우승자를 결정하는 코드를 분리 
- `RaceUtil`
  - Race 에 관련된 여러 유틸 함수들을 모아놓은 클래스
  - 문자열을 파싱하거나, 우승자를 결정하는 함수 등, Race 를 진행하는 데에는 직접적인 관련은 없지만 간접적으로 관련이 있는 함수들을 분리 
- `ConsoleWriter`
  - Console 에 결과를 출력하는 클래스

### fix: getWinners() 메서드에서 우승자 배열 값이 제대로 나오지 않는 문제 해결

### style: 자바 코드 컨벤션 적용 

### docs: 코드에 주석 추가 

### fix: 우승자 목록을 문자열로 변환하는 코드 간결화 